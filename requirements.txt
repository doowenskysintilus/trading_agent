# ===========================================================================
# quant-fund-ai — Python Dependencies
# ===========================================================================
# Install all:   pip install -r requirements.txt
# Install core:  pip install -r requirements.txt --no-deps  (then add selectively)
#
# Note: MetaTrader5 is Windows-only. Remove that line on Linux/macOS.
# Note: torch version depends on your CUDA version — see pytorch.org/get-started
# ===========================================================================

# ── Core data ───────────────────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.2.0
pyarrow>=15.0.0          # Parquet feature store (FeatureStore)
scipy>=1.12.0            # Statistics (backtesting/metrics.py)

# ── Machine Learning / Reinforcement Learning ───────────────────────────────
torch>=2.2.0             # Neural network backbone (ppo_lstm_policy.py)
gymnasium>=0.29.0        # RL environment (RLTradingEnv)
stable-baselines3>=2.3.0 # PPO training loop (rl_trainer.py)
sb3-contrib>=2.3.0       # RecurrentPPO with LSTM state (rl_trainer.py)
tensorboard>=2.16.0      # RL training metrics logging (rl_trainer.py)

# ── API / Web ────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0   # ASGI server with WebSocket support
pydantic>=2.6.0
python-dotenv>=1.0.0        # Loads .env configuration (config/settings.py)

# ── Database ────────────────────────────────────────────────────────────────
mysql-connector-python>=8.4.0   # Primary metrics store (monitoring/metrics_store.py)

# ── Broker / Market data ─────────────────────────────────────────────────────
MetaTrader5>=5.0.45 ; sys_platform == "win32"   # MT5 execution + data feed

# ── Optional: feature engineering helpers ───────────────────────────────────
scikit-learn>=1.4.0      # Used for normalisation / PCA in feature_store

# ── Development / Testing ───────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0            # FastAPI TestClient
